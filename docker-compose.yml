version: "3.0"
services:
  newman-script:
    container_name: newman-script
    network_mode: host
    build:
      context: .
      dockerfile: Dockerfile
    deploy:
      restart_policy:
        condition: on-failure
        max_attempts: 3
    ports:
      - "4044"
    volumes:
      - .:/app
